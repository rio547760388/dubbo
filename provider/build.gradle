plugins {
    id 'java'
}

group 'com.tian'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":service"))
}

archivesBaseName = 'provider'

jar.enabled = true
bootJar.enabled(true)
